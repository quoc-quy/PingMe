import type { Conversation } from "@/types/chat";
import React from "react";

const DirectMessageCard = ({ convo }: { convo: Conversation }) => {
    return <div>DirectMessageCard</div>;
};

export default DirectMessageCard;
